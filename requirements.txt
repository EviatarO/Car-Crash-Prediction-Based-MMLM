# ============================================================
# Car-Crash Prediction Based MMLM — Requirements
# ============================================================
# Install: pip install -r requirements.txt
# Tested with Python 3.10+
#   RTX 5090 (recommended): CUDA 12.8+, PyTorch 2.8+
#   RTX 4090 (fallback):    CUDA 12.1+, PyTorch 2.1+
# Training requires GPU with >= 16GB VRAM (RunPod RTX 5090 recommended)
# Zero-shot inference requires >= 10GB VRAM (RunPod) or CPU (slow)
#
# NOTE: flash-attn must be installed separately (requires CUDA compiler):
#   pip install flash-attn --no-build-isolation
# NOTE: torch/torchvision are pre-installed on RunPod — skip if already present.
#       For RTX 5090, select a RunPod template with PyTorch 2.8+ (CUDA 12.8).

# --- Core deep learning ---
torch>=2.8.0        # RTX 5090 (Blackwell) requires PyTorch 2.8+
torchvision>=0.20.0
accelerate>=0.27.0

# --- HuggingFace ecosystem ---
transformers>=4.44.0
peft>=0.10.0          # LoRA fine-tuning (REQUIRED for training)
einops>=0.7.0         # Required by InternVL attention layers
sentencepiece>=0.1.99 # Tokenizer dependency for some InternVL configs

# --- Image processing ---
Pillow>=10.0.0
opencv-python>=4.8.0

# --- Data & numerics ---
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0   # AP, AUC-ROC, confusion matrix

# --- Visualizations ---
matplotlib>=3.7.0
seaborn>=0.12.0

# --- Reasoning quality metrics ---
rouge-score>=0.1.2    # ROUGE-L for reasoning evaluation
bert-score>=0.3.13    # BERTScore for semantic similarity

# --- API & Teacher pipeline ---
python-dotenv>=1.0.0
openai>=1.0.0
requests>=2.31.0

# --- Utilities ---
openpyxl>=3.1.0       # Excel I/O for teacher outputs
tqdm>=4.65.0          # Progress bars (REQUIRED for training loop)
pyyaml>=6.0           # YAML config loading (REQUIRED for train_lora.yaml)
timm>=0.9.0           # Vision model utilities (InternVL dependency)
